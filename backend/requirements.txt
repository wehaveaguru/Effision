fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# storage
supabase==2.7.4          # optional — only needed if SUPABASE_URL/KEY are set

# pipeline
llama-cloud-services==0.6.94   # optional — only needed for .pdf/.docx parsing
groq==0.11.0                   # optional — only needed for real segregation  # embedding model (all-MiniLM-L6-v2, 384-dim)
pandas>=2.0.0                  # CSV ingestion for Unihack dataset

# explainability report
jinja2==3.1.4
weasyprint==62.3               # optional — only needed to render the PDF

# testing
pytest==8.3.3
httpx==0.27.2