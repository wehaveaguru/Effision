# =============================================================================
# Effision PIM — Production requirements (Vercel-safe, ~15 MB bundle)
# Heavy packages removed: torch, sentence-transformers, transformers,
#   scipy, scikit-learn, llama-cloud-services, weasyprint, pandas
# Embeddings now use Groq HTTP API (nomic-embed-text-v1.5) via httpx.
# =============================================================================

# --- Core API server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# --- HTTP client (used for Groq embedding API calls) ---
httpx>=0.27.0

# --- Supabase (pgvector storage + RPC) ---
supabase==2.7.4

# --- AI enrichment (LLM calls only — no local model weights) ---
groq==0.11.0

# --- Templating (used for report rendering without WeasyPrint) ---
jinja2==3.1.4